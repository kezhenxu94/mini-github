@import '../../style/base.wxss';

.notif {
  border-bottom: 1rpx solid rgba(65, 65, 65, 0.9);
}

.loading {
  text-align: center;
  margin: 24rpx 0;
}

.empty {
  display: flex;
  flex-direction: column;
  text-align: center;
}

.empty image {
  width: 100%;
  height: 60rpx;
  margin: 24rpx 0;
}